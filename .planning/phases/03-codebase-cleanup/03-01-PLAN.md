---
phase: 03-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/ai/formation/exercise-reviewer.ts
  - packages/core/src/ai/formation/index.ts
  - packages/core/src/ai/index.ts
  - packages/core/src/types/index.ts
  - packages/bot-discord/src/handlers/review-buttons.ts
  - packages/bot-discord/src/commands/admin/review.ts
  - packages/bot-discord/src/commands/admin/approve.ts
  - packages/bot-discord/src/commands/admin/revision.ts
  - packages/bot-discord/src/utils/format.ts
  - packages/core/src/db/formation/exercises.integration.test.ts
  - packages/bot-discord/src/commands/admin/approve.test.ts
  - packages/bot-discord/src/commands/admin/review.test.ts
  - packages/bot-discord/src/handlers/review-buttons.test.ts
  - supabase/migrations/020_remove_ai_reviewed_status.sql
  - specs/01-cerveau-central/SPEC.md
  - specs/04-bot-discord/SPEC.md
  - specs/04-bot-discord/SPEC-DM-AGENT.md
  - specs/00-infrastructure/SPEC.md
  - specs/ROADMAP.md
autonomous: true
requirements: [CLEAN-06]

must_haves:
  truths:
    - "The file exercise-reviewer.ts does not exist in the monorepo"
    - "No TypeScript file imports or references exercise-reviewer"
    - "No TypeScript file contains the string ai_reviewed"
    - "pnpm typecheck passes with zero errors for core and bot-discord"
    - "pnpm test:unit passes with zero failures"
    - "A SQL migration exists that converts ai_reviewed records to submitted"
    - "No spec file references exercise-reviewer as an active module"
  artifacts:
    - path: "supabase/migrations/020_remove_ai_reviewed_status.sql"
      provides: "DB migration converting ai_reviewed to submitted"
      contains: "UPDATE student_exercises SET status"
    - path: "packages/core/src/types/index.ts"
      provides: "ExerciseStatus type without ai_reviewed"
    - path: "packages/core/src/ai/formation/index.ts"
      provides: "Barrel export without reviewExercise"
    - path: "packages/core/src/ai/index.ts"
      provides: "Barrel export without reviewExercise"
  key_links:
    - from: "packages/core/src/types/index.ts"
      to: "all files using ExerciseStatus"
      via: "TypeScript type union"
      pattern: "ExerciseStatus.*submitted.*reviewed.*approved.*revision_needed.*archived"
---

<objective>
Delete the exercise-reviewer module, remove all ai_reviewed references from code/tests/specs, and create a DB migration for orphaned records.

Purpose: Final codebase cleanup (CLEAN-06) -- eliminate all dead code artifacts from the AI auto-review removal completed in Phases 1-2. After this plan, zero traces of exercise-reviewer or ai_reviewed remain.

Output: Clean codebase with no dead exercise-reviewer code, updated specs, and a DB migration.
</objective>

<execution_context>
@/Users/magomedsouleymanov/Documents/MAGA/MyProjects/vibe coder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magomedsouleymanov/Documents/MAGA/MyProjects/vibe coder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-codebase-cleanup/03-CONTEXT.md

<interfaces>
<!-- Key types and barrel exports the executor needs to understand before making changes. -->

From packages/core/src/types/index.ts (line 91):
```typescript
export type ExerciseStatus = 'submitted' | 'ai_reviewed' | 'reviewed' | 'approved' | 'revision_needed' | 'archived';
```

From packages/core/src/ai/formation/index.ts (lines 1-2):
```typescript
export { reviewExercise } from './exercise-reviewer.js';
export type { ExerciseReviewResult } from './exercise-reviewer.js';
```

From packages/core/src/ai/index.ts (lines 21-22):
```typescript
export { reviewExercise } from './formation/exercise-reviewer.js';
export type { ExerciseReviewResult } from './formation/exercise-reviewer.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete exercise-reviewer module and purge all ai_reviewed references from code and tests</name>
  <files>
    packages/core/src/ai/formation/exercise-reviewer.ts (DELETE)
    packages/core/src/ai/formation/index.ts
    packages/core/src/ai/index.ts
    packages/core/src/types/index.ts
    packages/bot-discord/src/handlers/review-buttons.ts
    packages/bot-discord/src/commands/admin/review.ts
    packages/bot-discord/src/commands/admin/approve.ts
    packages/bot-discord/src/commands/admin/revision.ts
    packages/bot-discord/src/utils/format.ts
    packages/core/src/db/formation/exercises.integration.test.ts
    packages/bot-discord/src/commands/admin/approve.test.ts
    packages/bot-discord/src/commands/admin/review.test.ts
    packages/bot-discord/src/handlers/review-buttons.test.ts
  </files>
  <read_first>
    packages/core/src/ai/formation/exercise-reviewer.ts
    packages/core/src/ai/formation/index.ts
    packages/core/src/ai/index.ts
    packages/core/src/types/index.ts
    packages/bot-discord/src/handlers/review-buttons.ts
    packages/bot-discord/src/commands/admin/review.ts
    packages/bot-discord/src/commands/admin/approve.ts
    packages/bot-discord/src/commands/admin/revision.ts
    packages/bot-discord/src/utils/format.ts
    packages/core/src/db/formation/exercises.integration.test.ts
    packages/bot-discord/src/commands/admin/approve.test.ts
    packages/bot-discord/src/commands/admin/review.test.ts
    packages/bot-discord/src/handlers/review-buttons.test.ts
  </read_first>
  <action>
    All changes below must be done atomically (all files in one pass) so that `pnpm typecheck` passes after.

    **1. Delete the module (per D-01):**
    Delete `packages/core/src/ai/formation/exercise-reviewer.ts` entirely.

    **2. Clean barrel exports (per D-02, D-03, D-04):**

    In `packages/core/src/ai/formation/index.ts`: Remove lines 1-2 (the two exercise-reviewer re-exports). Keep all other exports (faq-agent, dm-agent) untouched. The file should start with:
    ```typescript
    export { answerFaqQuestion } from './faq-agent.js';
    ```

    In `packages/core/src/ai/index.ts`: Remove lines 21-22 (the two exercise-reviewer re-exports). Keep all other exports untouched. The line after `export type { PlannedNotification }...` should be `export { answerFaqQuestion }...`.

    **3. Remove ai_reviewed from TypeScript type union (per D-05):**

    In `packages/core/src/types/index.ts` line 91, change:
    ```typescript
    export type ExerciseStatus = 'submitted' | 'ai_reviewed' | 'reviewed' | 'approved' | 'revision_needed' | 'archived';
    ```
    to:
    ```typescript
    export type ExerciseStatus = 'submitted' | 'reviewed' | 'approved' | 'revision_needed' | 'archived';
    ```

    **4. Remove ai_reviewed from production code (per D-07):**

    In `packages/bot-discord/src/handlers/review-buttons.ts`:
    - Line 56: Change `if (exercise.status !== 'submitted' && exercise.status !== 'ai_reviewed')` to `if (exercise.status !== 'submitted')`.
    - Line 121: Change `if (exercise.status !== 'submitted' && exercise.status !== 'ai_reviewed')` to `if (exercise.status !== 'submitted')`.

    In `packages/bot-discord/src/commands/admin/review.ts`:
    - Lines 91-95: Replace the block that builds each line with ai_reviewed emoji logic. Remove the `aiReview` variable (line 91), the `score`/`rec` variables (lines 92-93), the ternary emoji based on `ai_reviewed` (line 94), and the `scoreStr` (line 95). Replace with a simpler line:
      ```typescript
      const resubLabel = ex.submission_count > 1 ? ` (#${ex.submission_count})` : '';
      return `📩 **${name}**${resubLabel}`;
      ```
    - Line 145: Change `const pending = exercises.filter((e) => e.status === 'submitted' || e.status === 'ai_reviewed');` to `const pending = exercises.filter((e) => e.status === 'submitted');`.

    In `packages/bot-discord/src/commands/admin/approve.ts`:
    - Line 38: Change `let pending = exercises.filter((e) => e.status === 'submitted' || e.status === 'ai_reviewed');` to `let pending = exercises.filter((e) => e.status === 'submitted');`.

    In `packages/bot-discord/src/commands/admin/revision.ts`:
    - Line 38: Change `let pending = exercises.filter((e) => e.status === 'submitted' || e.status === 'ai_reviewed');` to `let pending = exercises.filter((e) => e.status === 'submitted');`.

    In `packages/bot-discord/src/utils/format.ts`:
    - Line 12: Remove `ai_reviewed: '🤖',` from the STATUS_EMOJI record. The entry after `submitted: '📩',` should be `reviewed: '👁️',`.

    **5. Update test files (per D-08):**

    In `packages/core/src/db/formation/exercises.integration.test.ts`:
    - Line 175: Change `expect(results.every((e) => ['submitted', 'ai_reviewed'].includes(e.status))).toBe(true);` to `expect(results.every((e) => e.status === 'submitted')).toBe(true);`.

    In `packages/bot-discord/src/commands/admin/approve.test.ts`:
    - Line 122: Change `status: 'ai_reviewed',` to `status: 'submitted',`.

    In `packages/bot-discord/src/commands/admin/review.test.ts`:
    - Line 103: Change `const exercise2 = createExerciseFixture({ exercise_number: 1, status: 'ai_reviewed' });` to `const exercise2 = createExerciseFixture({ exercise_number: 1, status: 'submitted' });`.

    In `packages/bot-discord/src/handlers/review-buttons.test.ts`:
    - Line 379: Update test description — change `'review_open_ returns error when exercise is already processed (not submitted/ai_reviewed)'` to `'review_open_ returns error when exercise is already processed (not submitted)'`.
    - Line 452: Change `status: 'ai_reviewed',` to `status: 'submitted',`.
    - Line 538: Change `createExercise({ id: 'ex-2', student_id: 'st-2', status: 'ai_reviewed' })` to `createExercise({ id: 'ex-2', student_id: 'st-2', status: 'submitted' })`.

    **6. Final check:** Run `pnpm typecheck` and `pnpm test:unit` to confirm zero errors.
  </action>
  <verify>
    <automated>cd "/Users/magomedsouleymanov/Documents/MAGA/MyProjects/vibe coder" && pnpm typecheck && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/ai/formation/exercise-reviewer.ts` does not exist (ls returns error)
    - `grep -r 'exercise-reviewer' packages/ --include='*.ts'` returns zero matches
    - `grep -r 'ai_reviewed' packages/ --include='*.ts'` returns zero matches
    - `grep -r 'reviewExercise' packages/ --include='*.ts'` returns zero matches
    - `grep -r 'ExerciseReviewResult' packages/ --include='*.ts'` returns zero matches
    - `packages/core/src/types/index.ts` contains `export type ExerciseStatus = 'submitted' | 'reviewed' | 'approved' | 'revision_needed' | 'archived';`
    - `packages/core/src/ai/formation/index.ts` does NOT contain `exercise-reviewer`
    - `packages/core/src/ai/index.ts` does NOT contain `exercise-reviewer`
    - `pnpm typecheck` exits 0
    - `pnpm test:unit` exits 0
  </acceptance_criteria>
  <done>
    exercise-reviewer.ts deleted. All barrel exports, type union references, production code filters, and test fixtures referencing ai_reviewed or exercise-reviewer are removed. TypeScript compiles cleanly and all unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DB migration and update spec files</name>
  <files>
    supabase/migrations/020_remove_ai_reviewed_status.sql
    specs/01-cerveau-central/SPEC.md
    specs/04-bot-discord/SPEC.md
    specs/04-bot-discord/SPEC-DM-AGENT.md
    specs/00-infrastructure/SPEC.md
    specs/ROADMAP.md
  </files>
  <read_first>
    supabase/migrations/019_architecture_fixes.sql
    specs/01-cerveau-central/SPEC.md
    specs/04-bot-discord/SPEC.md
    specs/04-bot-discord/SPEC-DM-AGENT.md
    specs/00-infrastructure/SPEC.md
    specs/ROADMAP.md
  </read_first>
  <action>
    **1. Create SQL migration (per D-06):**

    Create `supabase/migrations/020_remove_ai_reviewed_status.sql` with:
    ```sql
    -- Migration: Remove ai_reviewed status from student_exercises
    -- Any exercises still in ai_reviewed status (legacy from AI auto-review)
    -- are moved to submitted so they appear in the admin pending queue.
    UPDATE student_exercises
    SET status = 'submitted'
    WHERE status = 'ai_reviewed';
    ```

    The filename follows the existing convention: `NNN_description.sql` where the last file is `019_architecture_fixes.sql`, so this is `020`.

    **2. Update specs/01-cerveau-central/SPEC.md (per D-11):**

    At line 233, remove the Exercise Reviewer section entirely (lines 233-234):
    ```
    #### Exercise Reviewer ✅ (`formation/exercise-reviewer.ts`)
    Pre-review IA des exercices soumis. Score 1-10, points forts/ameliorations, recommendation.
    ```
    Replace with nothing (delete both lines). The section `### 2.11 Agents Formation (formation/)` should go directly from its heading to `#### FAQ Agent`.

    **3. Update specs/04-bot-discord/SPEC.md (per D-10):**

    Multiple locations:

    a) Lines 126-132 (submission flow): Replace the flow block to remove AI references:
    ```
    **Flow :**
    ```
    Etudiant soumet via DM (ou /submit lien:... module:N exercice:N)
    Bot : Exercice recu !
      → Log dans student_exercises (status: 'submitted')
      → Event 'exercise_submitted' cree (→ digest Telegram admin)
    ```
    ```

    b) Lines 143-144 (status table): Remove the `ai_reviewed` row entirely:
    ```
    | `ai_reviewed` | Pre-review IA (legacy, backward compat) |
    ```
    Delete this line so the table goes from `submitted` directly to `reviewed`.

    c) Line 239 (imports autorises): Change `core/src/ai/formation/ — exercise-reviewer, faq-agent` to `core/src/ai/formation/ — faq-agent, dm-agent, tsarag-agent`.

    d) Line 298 (agent checklist): Remove the line:
    ```
    - Agent de pre-review d'exercices (`core/src/ai/formation/exercise-reviewer.ts`) ✅
    ```

    **4. Update specs/04-bot-discord/SPEC-DM-AGENT.md:**

    a) Lines 536-548 (submission flow example): Replace the AI review lines. Change:
    ```
        → Lance reviewExercise() en arriere-plan
        → Cree event 'exercise_submitted' pour Telegram

    13. Claude repond :
        "✅ Задание по Сессии 2 отправлено! ИИ-проверка через несколько секунд..."

    14. L'IA termine la review (en arriere-plan) :
        → Met a jour student_exercises.ai_review + status = 'ai_reviewed'
        → Le bot envoie un DM a l'etudiant :
        "ИИ-проверка — Сессия 2 — Оценка: 7/10
         ✓ Bonne decomposition
         ✗ Manque les recettes
         Тренер завершит проверку."
    ```
    to:
    ```
        → Cree event 'exercise_submitted' pour Telegram

    13. Claude repond :
        "✅ Задание по Сессии 2 отправлено! Тренер проверит в ближайшее время."
    ```

    b) Lines 714-719 (error case "La review IA echoue"): Remove the entire error case section since AI review no longer exists. Delete from `### La review IA echoue` through the three bullet lines ending with `manuellement`.

    **5. Update specs/00-infrastructure/SPEC.md:**

    Line 138: Change the status list from:
    ```
    - status TEXT ('submitted', 'ai_reviewed', 'reviewed', 'approved', 'revision_needed')
    ```
    to:
    ```
    - status TEXT ('submitted', 'reviewed', 'approved', 'revision_needed', 'archived')
    ```

    **6. Update specs/ROADMAP.md:**

    Line 97: Change `- [x] Pre-review IA des exercices (exercise-reviewer.ts)` to `- [x] ~~Pre-review IA des exercices~~ (supprime — Phase 3 cleanup)`.
  </action>
  <verify>
    <automated>cd "/Users/magomedsouleymanov/Documents/MAGA/MyProjects/vibe coder" && test -f supabase/migrations/020_remove_ai_reviewed_status.sql && grep -q "UPDATE student_exercises" supabase/migrations/020_remove_ai_reviewed_status.sql && ! grep -q "exercise-reviewer" specs/01-cerveau-central/SPEC.md && ! grep -q "exercise-reviewer" specs/04-bot-discord/SPEC.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/020_remove_ai_reviewed_status.sql` exists and contains `UPDATE student_exercises SET status = 'submitted' WHERE status = 'ai_reviewed'`
    - `grep 'exercise-reviewer' specs/01-cerveau-central/SPEC.md` returns zero matches
    - `grep 'exercise-reviewer' specs/04-bot-discord/SPEC.md` returns zero matches (except the strikethrough note in ROADMAP)
    - `grep 'ai_reviewed' specs/04-bot-discord/SPEC.md` returns zero matches
    - `grep 'ai_reviewed' specs/04-bot-discord/SPEC-DM-AGENT.md` returns zero matches
    - `grep 'ai_reviewed' specs/00-infrastructure/SPEC.md` returns zero matches
    - `specs/00-infrastructure/SPEC.md` contains `status TEXT ('submitted', 'reviewed', 'approved', 'revision_needed', 'archived')`
    - `specs/ROADMAP.md` line 97 contains `~~Pre-review IA des exercices~~`
    - `specs/04-bot-discord/SPEC.md` imports autorises section lists `faq-agent, dm-agent, tsarag-agent` (not exercise-reviewer)
  </acceptance_criteria>
  <done>
    SQL migration created for converting legacy ai_reviewed records to submitted. All spec files updated: exercise-reviewer references removed from cerveau-central and bot-discord specs, ai_reviewed removed from status tables and flow descriptions, infrastructure spec updated with current status list including archived.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run full verification:

1. `pnpm typecheck` -- zero errors for all packages
2. `pnpm test:unit` -- all tests pass
3. `grep -r 'exercise-reviewer' packages/ --include='*.ts'` -- zero matches
4. `grep -r 'ai_reviewed' packages/ --include='*.ts'` -- zero matches
5. `grep -r 'reviewExercise' packages/ --include='*.ts'` -- zero matches
6. `grep -r 'ExerciseReviewResult' packages/ --include='*.ts'` -- zero matches
7. `grep -r 'exercise-reviewer' specs/` -- only the strikethrough line in specs/ROADMAP.md
8. `grep -r 'ai_reviewed' specs/` -- zero matches
9. `ls packages/core/src/ai/formation/exercise-reviewer.ts` -- file not found
10. `cat supabase/migrations/020_remove_ai_reviewed_status.sql` -- contains UPDATE statement
</verification>

<success_criteria>
- The exercise-reviewer module is deleted and no file in packages/ references it
- The string `ai_reviewed` appears nowhere in packages/ (code or tests)
- TypeScript compiles cleanly (`pnpm typecheck` exits 0)
- All unit tests pass (`pnpm test:unit` exits 0)
- A SQL migration exists to convert orphaned ai_reviewed records to submitted
- Spec files reflect the current state: no exercise-reviewer, no ai_reviewed status
</success_criteria>

<output>
After completion, create `.planning/phases/03-codebase-cleanup/03-01-SUMMARY.md`
</output>
